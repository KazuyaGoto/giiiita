<div class="container">
  <h1>ユーザページ</h1>
  <div class="shadow p-3 mb-5 bg-white rounded">
    <p> 写真 : <%= image_tag(@user.image_url) if @user.image.present? %> </p>
    <p>名前 : <%= @user.name %></p>
    <p>メールアドレス : <%= @user.email %></p>
    <p>プロフィール : <%= @user.profile %></p>
    <p>slackの名前 :<%= @user.slack_name %></p>
    <% if current_user.id == @user.id %>
    <%= link_to "編集する", edit_user_registration_path %>
  <% end %>
  </div>
  <div class="row">
    <div class="shadow col-md-6 bg-white rounded">
    <h2>ユーザーの投稿一覧</h2>
      <% @user.posts.each do |t| %>
        <p><%= t.user.name %>(<%= t.user.slack_name %>)</p>
        <p><%= t.body %></p>
        <%= link_to "投稿詳細へ", post_path(t.id) %>
      <% end %>
    </div>
    <div class="shadow col-md-6 bg-white rounded">
    <h2>ユーザーがいいねした投稿一覧</h2>
      <% @user.liked_posts.each do |t| %>
        <p><%= t.user.name %>(<%= t.user.slack_name %>)</p>
        <p><%= t.body %></p>
        <%= link_to "詳細へ", post_path(t.id) %>
      <% end %>
    </div> 
 </div>  
</div>

